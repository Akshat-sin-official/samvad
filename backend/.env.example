PROJECT_ID=brdsys
LOCATION=us-central1
# GOOGLE_APPLICATION_CREDENTIALS is not needed for Cloud Run - it uses Application Default Credentials automatically
# Only set this if running locally and you have a service account key file
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service_account_key.json
